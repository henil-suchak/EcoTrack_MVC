@model IEnumerable<EcoTrack.WebMvc.ViewModels.ActivityViewModel>

<!-- ... other HTML ... -->

<table class="table">
    <thead>
        <tr>
            <th>Date</th>
            <th>Description</th>
            <th>Carbon Emission (kg CO₂)</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var activity in Model)
        {
            <tr>
                <td>@activity.DateTime.ToShortDateString()</td>
                @* CORRECTED: Changed from .Description to .GetDescription() *@
                <td>@activity.GetDescription()</td>
                <td>@activity.CarbonEmission.ToString("F2")</td>
            </tr>
        }
    </tbody>
</table>

<!-- ... other HTML ... -->